// career-map — schemas

export {
  careerActionSchema,
  careerActionPlacementSchema,
  careerActionProgressSchema,
  careerActionStatusSchema,
  careerMapSchema,
  careerMapTermSchema,
  careerTrackSchema,
  studentCareerMapSchema,
} from '@/lib/canonical'

// Week 2 adds `markCareerActionSchema` and `setCareerTrackSchema` here — the
// same schemas the forms resolve against.
